using BuildingBlocks.Export;using BuildingBlocks.Grids;using Todos.Domain;namespace Todos.Contracts;/** <summary>Create.</summary> */ public sealed record CreateTodoRequest(string Title,DateOnly DueDate,TodoPriority Priority,string? Notes);/** <summary>Update.</summary> */ public sealed record UpdateTodoRequest(string Title,DateOnly DueDate,TodoPriority Priority,string? Notes);/** <summary>Status.</summary> */ public sealed record ChangeTodoStatusRequest(TodoStatus Status);/** <summary>Response.</summary> */ public sealed record TodoResponse(Guid Id,string Title,DateOnly DueDate,TodoStatus Status,TodoPriority Priority,string? Notes,DateTime CreatedAtUtc,DateTime UpdatedAtUtc);/** <summary>Export.</summary> */ public sealed record TodoExportRequest(GridQuery Grid,ExportFormat Format);
